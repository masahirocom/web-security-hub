'use strict';
import { api } from '../api.js'; import { $ } from '../dom.js'; import { renderFindings } from './renderFindings.js'; import { currentZapConfig } from './zapConnection.js';
function readScenario() { const text = $('scenarioJson').value.trim(); if (!text) return undefined; try { return JSON.parse(text); } catch { throw new Error('シナリオ JSON の形式が正しくありません'); } }
export function wireDynamicScan() { $('runDynamic').addEventListener('click', async () => { const status = $('dynamicStatus'); const result = $('dynamicResult'); status.textContent = '診断中… 完了までお待ちください'; result.innerHTML = ''; try { const zap = currentZapConfig(); const data = await api('/security/dynamic-scan', { targetUrl: $('targetUrl').value.trim(), maxPages: Number($('securityMaxPages').value), maxDepth: Number($('securityMaxDepth').value), allowActiveScan: $('activeScan').checked, authorizationConfirmed: $('authorized').checked, scenario: readScenario(), zap: zap.enabled ? zap : undefined }); status.textContent = `完了: ${data.pages.length} ページ・${data.requests.length} 通信を確認`; result.innerHTML = renderFindings(data, `${data.mode} / ${data.scannedAt}${data.zap ? ` / ZAP ${data.zap.version}` : ''}`); } catch (error) { status.textContent = `エラー: ${error.message}`; } }); }
