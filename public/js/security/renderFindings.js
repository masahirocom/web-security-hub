'use strict';
export function escapeHtml(value) { return String(value).replace(/[&<>'"]/g, (char) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' }[char])); }
export function renderFindings(result, details) { const summary = Object.entries(result.summary).filter(([, count]) => count).map(([severity, count]) => `<span class="severity ${severity}">${escapeHtml(severity)} ${count}</span>`).join(' '); const rows = result.findings.map((finding) => `<tr><td><span class="severity ${finding.severity}">${finding.severity}</span></td><td>${escapeHtml(finding.title)}</td><td><a href="${finding.owasp.url}" target="_blank" rel="noopener">${finding.owasp.id}</a><br><span class="hint">${escapeHtml(finding.owasp.name)}</span></td><td><code>${escapeHtml(finding.location)}</code></td><td>${escapeHtml(finding.evidence)}<br><span class="hint">修正: ${escapeHtml(finding.remediation)}</span></td></tr>`).join(''); return `<div class="scan-summary"><strong>${result.findings.length} 件の検出</strong>${summary}<span class="hint">${escapeHtml(details)}</span></div>${result.findings.length ? `<table class="findings"><thead><tr><th>重要度</th><th>検出内容</th><th>OWASP</th><th>場所</th><th>根拠 / 修正</th></tr></thead><tbody>${rows}</tbody></table>` : '<p class="result-ok">検出はありませんでした。</p>'}`; }
